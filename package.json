{
  "name": "react-use-page-visibility-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter example dev"
  },
  "devDependencies": {
    "pnpm": "^9.1.1"
  },
  "packageManager": "pnpm@9.1.1",
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
